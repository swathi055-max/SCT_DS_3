# -----------------------------
# CREATE bank.csv (Raw dataset)
# -----------------------------
import pandas as pd
from ucimlrepo import fetch_ucirepo
import os

# Make sure folder exists
os.makedirs("data/raw", exist_ok=True)

# Fetch the Bank Marketing dataset from UCI (ID = 222)
dataset = fetch_ucirepo(id=222)

# Combine features and target columns
X = dataset.data.features
y = dataset.data.targets
bank_df = pd.concat([X, y], axis=1)

# Save as CSV
bank_df.to_csv("data/raw/bank.csv", index=False)

print("✅ 'bank.csv' created successfully!")
print("Shape:", bank_df.shape)
print("Columns:", list(bank_df.columns))
